# Lives here, not in pyproject.toml: the repository root is itself the plugin
# package (it has an __init__.py with relative imports), and pytest would try
# to import it as a test package if the root were the rootdir.
# Run with:  pytest tests
[pytest]
addopts = -q
